<div class="field">
<%= form_for @topic do |f| %>

  <% if @topic.errors.any? %>
     <div id="error_explanation" class="has-warning">
      <h4><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved.</h4> 
        <% @topic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
     </div>
  <% end %>


  <%= f.label "New topic" %>
  <%= f.text_field :title %>
  
  <%= f.submit "Save" %>



 
<% end %>
</div>

