<% if current_user %>
<%= form_for [topic, bookmark] do |f| %>
  <%= f.label :url, "URL" %>
  <%= f.text_field :url, value: @preview.site_url %>
  <%= image_tag(@preview.image_url) if @preview %>
  <%= f.submit "Preview" %>
  <%= f.submit "Save" %>

<% end %>
<% end %>