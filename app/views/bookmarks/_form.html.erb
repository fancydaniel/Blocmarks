<% if current_user %>
<%= form_for [topic, bookmark] do |f| %>
  <%= f.label :url, "URL" %>
  <%= f.text_field :url %>
  <%= @preview if @preview %>
  <%= f.submit "Preview" %>
  <%= f.submit "Save" %>

<% end %>
<% end %>